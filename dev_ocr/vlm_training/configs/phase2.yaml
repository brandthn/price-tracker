# Phase 2 : on ouvre les LoRA. Le projecteur est deja degrossi, donc il ne pousse
# plus de bruit dans le decodeur.
phase: 2
lr: 1.0e-4
epochs: 10
trainable: [projector, lora_]
sources: [synthetic, real]
