# Secrets
.env

# Moondream VLM weights (large binaries — never commit)
data/models/
data/benchmarks/
*.mf
*.mf.gz

# receipt_vlm training artefacts (large binaries / generated data — never commit)
vlm_training/colab_upload/
vlm_training/data/
vlm_training/runs/
vlm_training/.venv/
*.pt
*.ckpt
