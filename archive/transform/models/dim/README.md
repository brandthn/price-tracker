README: models/dim

- Contient les tables dimensionnelles (produit, enseigne, catégories COICOP, etc.)
- Exemples: `dim_product`, `dim_brand`, `dim_store`.
- Ces modèles sont construits avec dbt et écrits dans le dataset `silver`.
