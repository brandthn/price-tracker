README: models/fact

- Contient les faits temporels, ex: `fact_price_observations` (prix observés par EAN, date, enseigne).
- Construit à partir des tables `silver.open_prices_clean` et `silver.catalogue_produits`.
