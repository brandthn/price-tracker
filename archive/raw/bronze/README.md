Bronze (raw):

- Emplacement: GCS (parquet) ou local `raw/bronze/` pour développement.
- Contenu: fichiers Parquet exportés depuis les ingestions brutes (open prices, photos tickets, exports HF).
- Usage: source d'ingestion vers `silver` via jobs ETL/Cloud Run.
