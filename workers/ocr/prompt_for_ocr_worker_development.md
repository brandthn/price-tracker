I need to create a complex prompt that will allow you to develop a solution without having to scan multiple times the codebase.

By reading the codebase present in  the dev_ocr folder, produce me a document in the documentation folder that summarizes how the codebase is structured, how to interact with the extract_receipt function, the input and output signature.

Specifically, the solution you'll have to develop will have to use the vlm backend, specifically the groq vlm provider.

The resulting document you'll produce will be fed to claude later, together with the desired solution architecture, in order to obtain the final prompt for you, so create this document with all the technical details that another llm would need to understand how its 

