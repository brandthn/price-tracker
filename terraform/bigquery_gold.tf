# Les tables Gold (`aggregatsenseignes`, `indicesinflation`, etc.) sont recalculées
# par `worker_indices` via des requêtes `CREATE OR REPLACE TABLE`.
# Aucune ressource BigQuery supplémentaire obligatoire ici.
